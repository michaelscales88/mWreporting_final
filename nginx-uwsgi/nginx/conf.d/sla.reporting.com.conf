# Application Server
upstream flask {
    server uwsgi:8001;
}

server {
    listen                  80;
    server_name             sla.reporting.com;
    access_log              /var/log/reporting.secure.access.log main;

    location / {
        proxy_pass          http://flask/;
        proxy_read_timeout  90;
    }
}
